import React from "react";
import styles from "../styles/main-header.module.css";

const MainHeader = () => {
  return <div className={`${styles.wrapper} + bg-black text-white`}></div>;
};

export default MainHeader;
